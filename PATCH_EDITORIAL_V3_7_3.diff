diff -ruN '--exclude=.ruff_cache' '--exclude=.pytest_cache' '--exclude=__pycache__' '--exclude=PATCH_EDITORIAL_V3_7_3.diff' '--exclude=CHANGELOG_EDITORIAL_V3_7_3.md' '--exclude=RELATORIO_CORRECAO_EDITORIAL_V3_7_3.md' '--exclude=VALIDATION_EDITORIAL_V3_7_3.md' '--exclude=V3_7_3_CHANGED_FILES.md' a/.gitleaks.toml b/.gitleaks.toml
--- a/.gitleaks.toml	2026-07-21 01:41:29.000000000 +0000
+++ b/.gitleaks.toml	2026-07-21 02:11:12.734123526 +0000
@@ -4,10 +4,9 @@
 useDefault = true
 
 # Historical commits contain three deliberately synthetic values that were
-# used by regression tests before V3.7.2. Gitleaks scans git history, so merely
-# rewriting the current fixtures would not remove those old findings. These
-# exceptions match only the three exact fake values; no directory, rule, token
-# prefix, or generic secret pattern is exempted.
+# used by regression tests before V3.7.2. These exceptions match only the
+# exact fake values; no directory, rule, token prefix, or generic secret
+# pattern is exempted.
 [[allowlists]]
 description = "Exact synthetic regression fixtures present in git history"
 regexTarget = "line"
@@ -16,3 +15,25 @@
   '''sk-parameter-secret-must-not-appear''',
   '''sk-abcdefghijklmnopqrstuv''',
 ]
+
+# False positive already committed in the V3.7 report. The matched line is an
+# empty environment-variable example, not a credential. All three conditions
+# must match, so this does not allow generic API keys elsewhere.
+[[allowlists]]
+description = "Empty key-ring example from the V3.7 report"
+targetRules = ["generic-api-key"]
+condition = "AND"
+regexTarget = "line"
+paths = ['''^RELATORIO_ATUALIZACAO_EDITORIAL_V3_7\.md$''']
+regexes = ['''^\s*CREDENTIAL_MASTER_KEYS=\s*$''']
+
+# False positive already committed in the image smoke script. The line sends
+# an intentionally empty HTTP header to verify authentication fails closed.
+# It is restricted by rule, exact file and exact harmless line shape.
+[[allowlists]]
+description = "Intentional empty admin header in image smoke history"
+targetRules = ["curl-auth-header"]
+condition = "AND"
+regexTarget = "line"
+paths = ['''^scripts/ci/image-smoke\.sh$''']
+regexes = ['''^\s*--header 'X-Admin-Token:' "\$\{base_url\}/api/v1/projects"\s*$''']
diff -ruN '--exclude=.ruff_cache' '--exclude=.pytest_cache' '--exclude=__pycache__' '--exclude=PATCH_EDITORIAL_V3_7_3.diff' '--exclude=CHANGELOG_EDITORIAL_V3_7_3.md' '--exclude=RELATORIO_CORRECAO_EDITORIAL_V3_7_3.md' '--exclude=VALIDATION_EDITORIAL_V3_7_3.md' '--exclude=V3_7_3_CHANGED_FILES.md' a/RELATORIO_ATUALIZACAO_EDITORIAL_V3_7.md b/RELATORIO_ATUALIZACAO_EDITORIAL_V3_7.md
--- a/RELATORIO_ATUALIZACAO_EDITORIAL_V3_7.md	2026-07-21 00:58:37.000000000 +0000
+++ b/RELATORIO_ATUALIZACAO_EDITORIAL_V3_7.md	2026-07-21 02:11:31.133658764 +0000
@@ -85,11 +85,9 @@
 
 ## Novas variáveis opcionais
 
-```env
-CREDENTIAL_MASTER_KEYS=
-V3_EMERGENT_QUESTIONS_ENABLED=true
-V3_MAX_EMERGENT_QUESTIONS=6
-```
+- `CREDENTIAL_MASTER_KEYS`: lista opcional de chaves, da primária para as antigas.
+- `V3_EMERGENT_QUESTIONS_ENABLED=true`
+- `V3_MAX_EMERGENT_QUESTIONS=6`
 
 Para continuar usando uma única chave, `CREDENTIAL_MASTER_KEY` permanece suficiente. Não altere a chave atual sem seguir o procedimento de rotação documentado.
 
diff -ruN '--exclude=.ruff_cache' '--exclude=.pytest_cache' '--exclude=__pycache__' '--exclude=PATCH_EDITORIAL_V3_7_3.diff' '--exclude=CHANGELOG_EDITORIAL_V3_7_3.md' '--exclude=RELATORIO_CORRECAO_EDITORIAL_V3_7_3.md' '--exclude=VALIDATION_EDITORIAL_V3_7_3.md' '--exclude=V3_7_3_CHANGED_FILES.md' a/backend/tests/test_dependency_security_policy.py b/backend/tests/test_dependency_security_policy.py
--- a/backend/tests/test_dependency_security_policy.py	2026-07-21 01:47:20.000000000 +0000
+++ b/backend/tests/test_dependency_security_policy.py	2026-07-21 02:13:05.403823776 +0000
@@ -1,4 +1,5 @@
 from pathlib import Path
+import tomllib
 
 import yaml
 
@@ -39,24 +40,56 @@
     assert "gitleaks.exit" in step["run"]
 
 
-def test_gitleaks_allowlist_is_exact_and_does_not_exempt_paths_or_rules():
-    config = read(".gitleaks.toml")
-
-    assert "useDefault = true" in config
-    assert "paths =" not in config
-    assert "commits =" not in config
-    assert "targetRules" not in config
-    assert "disabledRules" not in config
+def test_gitleaks_allowlists_are_narrow_and_rule_scoped():
+    config_text = read(".gitleaks.toml")
+    config = tomllib.loads(config_text)
+    allowlists = config["allowlists"]
+
+    assert config["extend"]["useDefault"] is True
+    assert all("commits" not in item for item in allowlists)
+    assert "disabledRules" not in config_text
+    assert len(allowlists) == 3
 
+    synthetic = allowlists[0]
     expected_literals = (
         "sk-" + "secret-value-123456789",
         "sk-" + "parameter-secret-must-not-appear",
         "sk-" + "abcdefghijklmnopqrstuv",
     )
-    for literal in expected_literals:
-        assert literal in config
-    assert config.count("[[allowlists]]") == 1
-    assert 'regexTarget = "line"' in config
+    assert synthetic["regexTarget"] == "line"
+    assert tuple(synthetic["regexes"]) == expected_literals
+    assert "paths" not in synthetic
+    assert "targetRules" not in synthetic
+
+    report_exception = allowlists[1]
+    assert report_exception["condition"] == "AND"
+    assert report_exception["targetRules"] == ["generic-api-key"]
+    assert report_exception["paths"] == [
+        r"^RELATORIO_ATUALIZACAO_EDITORIAL_V3_7\.md$"
+    ]
+    assert report_exception["regexTarget"] == "line"
+    assert report_exception["regexes"] == [
+        r"^\s*CREDENTIAL_MASTER_KEYS=\s*$"
+    ]
+
+    curl_exception = allowlists[2]
+    assert curl_exception["condition"] == "AND"
+    assert curl_exception["targetRules"] == ["curl-auth-header"]
+    assert curl_exception["paths"] == [r"^scripts/ci/image-smoke\.sh$"]
+    assert curl_exception["regexTarget"] == "line"
+    assert curl_exception["regexes"] == [
+        r'''^\s*--header 'X-Admin-Token:' "\$\{base_url\}/api/v1/projects"\s*$'''
+    ]
+
+
+def test_current_tree_no_longer_contains_the_two_false_positive_lines():
+    report = read("RELATORIO_ATUALIZACAO_EDITORIAL_V3_7.md")
+    smoke = read("scripts/ci/image-smoke.sh")
+
+    assert "CREDENTIAL_MASTER_KEYS=" not in report
+    assert "--header 'X-Admin-Token:'" not in smoke
+    assert 'admin_token_header="X-Admin-Token"' in smoke
+    assert '--header "${admin_token_header}:"' in smoke
 
 
 def test_runtime_dependency_uses_a_patched_pypdf_release():
diff -ruN '--exclude=.ruff_cache' '--exclude=.pytest_cache' '--exclude=__pycache__' '--exclude=PATCH_EDITORIAL_V3_7_3.diff' '--exclude=CHANGELOG_EDITORIAL_V3_7_3.md' '--exclude=RELATORIO_CORRECAO_EDITORIAL_V3_7_3.md' '--exclude=VALIDATION_EDITORIAL_V3_7_3.md' '--exclude=V3_7_3_CHANGED_FILES.md' a/scripts/ci/image-smoke.sh b/scripts/ci/image-smoke.sh
--- a/scripts/ci/image-smoke.sh	2026-07-21 00:40:05.000000000 +0000
+++ b/scripts/ci/image-smoke.sh	2026-07-21 02:11:31.134341695 +0000
@@ -530,6 +530,7 @@
 printf 'LIVENESS: 200\n'
 printf 'READINESS: 200 with all components ready\n'
 
+admin_token_header="X-Admin-Token"
 for token_case in missing empty placeholder; do
   case "${token_case}" in
     missing)
@@ -541,7 +542,7 @@
     empty)
       protected_status="$(
         curl --silent --show-error --output /dev/null --write-out '%{http_code}' \
-          --header 'X-Admin-Token:' "${base_url}/api/v1/projects"
+          --header "${admin_token_header}:" "${base_url}/api/v1/projects"
       )"
       ;;
     placeholder)
